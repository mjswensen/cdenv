//! Integration-test harness for environment-backed cdenv tests.
//!
//! Ordinary workspace tests stay environment-independent. Docker, Compose, and
//! OpenSSH tests are enabled only through `cargo xtask test-integration`.
