# Metadata and Feature-source fixture

Proves property-specific image/repository/Feature metadata merge semantics, repository-last conflicts, stable deduplication, lifecycle contribution order, contained local Features, verified HTTPS tarball transport (served by the gate's local TLS fixture), digest/size checks, and hostile archive rejection.
