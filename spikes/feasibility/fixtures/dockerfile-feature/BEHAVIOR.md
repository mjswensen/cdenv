# Dockerfile and Feature fixture

Proves context resolution and `.dockerignore`, BuildKit target/argument/supported-option handling, reserved-option rejection, anonymous public OCI Feature resolution, local recursive dependencies/options/order, one generated layer per Feature, lifecycle string/array/parallel forms, and `waitFor`.
