# Image fixture

Proves bounded JSONC parsing, image-scenario defaults, staged host/container substitutions, stable `${devcontainerId}`, published/forwarded port planning, and direct lifecycle command form without Node.js.
