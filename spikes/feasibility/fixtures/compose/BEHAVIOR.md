# Two-service Compose fixture

Proves deterministic overrides and cdenv labels, isolated project/resource identities for two workspaces, exact primary-service discovery, persisted managed-service stop, named-volume preservation, direct drift-safe resume without Compose reconciliation, and a declared forward targeting the dependency service.
